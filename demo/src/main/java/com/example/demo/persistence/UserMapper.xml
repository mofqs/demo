<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.persistence.UserMapper">
	<select id="getUserById" parameterType="Integer" resultType="com.example.demo.dto.User">
		SELECT * from form where id = #{id}
	</select>

<!-- 	<insert id="createUser" parameterType="User">
		INSERT INTO form (id,name) VALUES (#{id}, #{name})
	</insert>

	<update id="updateUser" parameterType="User">
		UPDATE form set name = #{name} where id = #{id}
	</update>

	<delete id="deleteUser">
		DELETE from form where id = #{id}
	</delete>

	<select id="getNumOfUser" resultType="int">
		SELECT count(*) from form
	</select> -->

</mapper>